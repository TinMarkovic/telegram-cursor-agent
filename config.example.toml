# Optional — bare-metal / advanced. Docker needs no config.toml (env only).

workspace_path = "/path/to/your/repo"
allowed_sender_ids = [123456789]
default_model = "claude-sonnet-5"
default_perms_mode = "standard"
perms_dir = "./perms"
session_store_dir = "./data"
