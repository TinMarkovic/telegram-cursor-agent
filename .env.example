# Copy to .env (gitignored) and fill in. Never commit real values.

# From BotFather after creating a new bot.
TELEGRAM_BOT_TOKEN=

# From the Cursor dashboard -> Integrations. Distinct from the device-login
# session used by the `agent` CLI — this is a separate auth path.
CURSOR_API_KEY=
